apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: simulate-agent-failure
  namespace: dev
spec:
  podSelector:
    matchLabels:
      app: agent
  policyTypes:
    - Ingress
  ingress: []
