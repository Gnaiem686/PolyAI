apiVersion: v1
kind: Service
metadata:
  name: img-proc-mcp-svc
  namespace: dev
spec:
  type: ClusterIP
  selector:
    app: img-proc-mcp
  ports:
    - protocol: TCP
      port: 8100
      targetPort: 8100
