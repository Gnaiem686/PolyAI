apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: agent
  namespace: dev
spec:
  ingressClassName: nginx
  rules:
    - host: agent-dev.gnaiem.fursa.click
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: agent-svc
                port:
                  number: 8000
